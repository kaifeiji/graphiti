"""Service layer package for the Graphiti backend.

The services in this package isolate graph storage, ArcGIS-compatible model
calls, and session persistence so the FastAPI entrypoint can remain minimal.
"""
